# Copy to .env and adjust as needed.
MEMOS_PORT=5230
TZ=Asia/Shanghai

# Optional: set at build time
# MEMOS_VERSION=dev
# MEMOS_COMMIT=local

# Optional: external database (default is SQLite in ./data)
# MEMOS_DRIVER=postgres
# MEMOS_DSN=postgresql://user:password@host:5432/memos?sslmode=disable
